<section class="solution" id="solution">
    <h2>{{title}}</h2>
    <div class="solution-grid">
        {{#each items}}
        <div class="solution-item">
            <h3>{{title}}</h3>
            <p>{{description}}</p>
        </div>
        {{/each}}
    </div>
</section>
